{
  "name": "tienda-mixify",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx serve -p 3000",
    "build": "echo 'Static site - no build required'",
    "start": "npx serve -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
